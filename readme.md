# f-serviceworker

Contains the registration script for `service-worker.js` located in the root of your website. Simply `require(@justeat/f-serviceworker)` on all pages which should be managed by the service worker and the registration script will run automatically.